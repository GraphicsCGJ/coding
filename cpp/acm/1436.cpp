#include <iostream>
#include <cmath>
#include<set>

using namespace std;


const int tmp = 666;

int main(void) {
    set<int, std::less<int>> s;

    /* insert */
    for (int i = 0; i < 5; i++) {
        int sum = 0;
        sum += (int)pow(10, i) * tmp;
        for ()
    }

    return 0;
}