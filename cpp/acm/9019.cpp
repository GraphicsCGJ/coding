#include <iostream>
#include <vector>

using namespace std;

int T;
int a, b;

class C1 {
public:
    int value;
    vector<int> q;
};

int main(void) {
    cin.tie(0);
    ios_base::sync_with_stdio(0);

    while(T--) {
        cin >> a >> b;



    }
    return 0;
}